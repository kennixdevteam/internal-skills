{
  "$comment": [
    "VS Code MCP config for the intranet Jira connector.",
    "Merge into your USER mcp.json (Command Palette > MCP: Open User Configuration,",
    "i.e. ~/Library/Application Support/Code/User/mcp.json) so it works in every workspace.",
    "If that file already has servers, add these entries instead of overwriting it.",
    "Then replace the absolute path and the JIRA_BASE_URL host.",
    "The token is prompted for and stored by VS Code, never written to this file."
  ],
  "inputs": [
    {
      "id": "jira-token",
      "type": "promptString",
      "description": "Jira personal access token",
      "password": true
    }
  ],
  "servers": {
    "jira-connector": {
      "type": "stdio",
      "command": "node",
      "args": ["/ABSOLUTE/PATH/TO/internal-skills/jira-connector/mcp/server.mjs"],
      "env": {
        "JIRA_BASE_URL": "https://jira.your-intranet.example",
        "JIRA_TOKEN": "${input:jira-token}"
      }
    }
  }
}
